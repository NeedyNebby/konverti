# konverti
konverti (konˈverti) or convert in english, is a local converting app which is a frontend for FFmpeg built using Python Tkinter
# Prerequisites
- FFmpeg
- Python
# Usage
The usage is pretty straightforward, Run the python script and select file, select the file button, the rest is pretty straight forward for you to figure it out yourself.
# Support
Currently I've only tested it on windows, but it should work on linux or mac.
